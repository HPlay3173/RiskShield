# RiskShield v0.4 AI Interpreter 하이브리드 제안

상태: 제안 및 검증 설계만 승인
선행 결정: v0.3.1 규칙 기반 단독 Analyzer 최종 FAIL

## 1. 목표

v0.4의 목표는 위험 키워드를 더 많이 추가하는 것이 아니라, 규칙이 찾은 표현을 **누가, 어떤 목적으로, 어떤 문맥에서 말했는지** 해석하는 것이다. 기존 규칙은 빠르고 설명 가능한 후보 검색·근거 하이라이트 계층으로 유지하고, AI Interpreter는 발화 목적과 문맥 관계를 구조화한다.

## 2. 제안 구조

1. 입력 정규화와 최소한의 개인정보 마스킹
2. 기존 규칙 엔진의 위험 후보·도메인·근거 구간 추출
3. AI Interpreter의 문맥 해석
4. 규칙 증거와 Interpreter 결과를 결합한 위험 등급 산정
5. 모호하거나 불일치하는 결과는 사람 검토로 전달

AI Interpreter는 최소한 다음을 구분해야 한다.

- 발화 유형: 직접 홍보 주장, 인용, 경고, 비판, 보도, 정의·설명
- 행위자와 대상: 누가 누구에게 무엇을 약속하거나 권유하는지
- 부정·대조·조건: `아니다`, `주의`, `보장되지 않는다`, 계약상 조건 등
- 주장 성격: 광고성 보장, 사실 보고, 계약 조건, 법정·제도상 보호
- 위험 의도: 구매·투자·진학·의료행위·정보 접근을 유도하는지

## 3. Interpreter 출력 계약

자유 문장이 아니라 검증 가능한 JSON만 반환한다.

```json
{
  "risk_intent": "direct_promotional | contextual_only | uncertain",
  "speech_act": "claim | quote | warning | criticism | report | definition",
  "claim_target": "health | finance | education | legal | privacy | general | none",
  "context_relation": "supports | negates | warns_about | reports | defines | unclear",
  "confidence": 0.0,
  "evidence_spans": [{ "start": 0, "end": 0, "text": "" }],
  "policy_reason": "short controlled reason code"
}
```

JSON 스키마 불일치, 타임아웃, 낮은 신뢰도, 규칙과의 강한 충돌은 자동 high가 아니라 review로 처리한다.

## 4. 결합 원칙

- high는 규칙의 구체적 위험 증거와 Interpreter의 직접 홍보·권유 의도가 함께 확인될 때만 허용한다.
- 인용, 경고, 비판, 보도, 정의 문맥은 원칙적으로 high를 억제하고 필요하면 review로 보낸다.
- 계약 조건·제도상 보호와 광고성 보장을 분리한다.
- 근거 구간과 이유 코드는 UI에서 계속 노출해 설명 가능성을 유지한다.
- AI 단독 판정을 최종 권위로 사용하지 않으며, 애매한 사례에는 사람 검토를 유지한다.

## 5. 개인정보·보안 기준

- 모델에 보내는 텍스트를 필요한 문장 범위로 최소화한다.
- 이메일, 전화번호, 계좌·주민 식별자 등 직접 식별정보를 사전 마스킹한다.
- 사용자 동의와 보존 정책을 명시하고, 입력을 모델 학습에 재사용하지 않는 구성을 우선한다.
- 공급자 오류나 Interpreter 실패 시 안전한 review로 폴백한다.
- 정규화된 입력 해시 기반 캐시는 원문을 저장하지 않고, 보존 기간과 삭제 정책을 둔다.

## 6. 지연시간·비용 제어

- 규칙 단계에서 위험 후보가 전혀 없고 안전 조건이 명확한 입력은 Interpreter 호출을 생략할 수 있다.
- 동일 정규화 입력은 짧은 TTL의 해시 캐시로 중복 호출을 줄인다.
- 문서 전체가 아닌 후보 주변 문장만 전달한다.
- 모델·프롬프트·스키마 버전을 결과에 기록해 재현성과 비용 회계를 가능하게 한다.

## 7. 평가 설계

구현 전에 아래 세트를 읽기 전용 평가 자산으로 고정한다.

- 기존 동결 100건
- 개발 60건
- 이번 v0.3.1 최종 신규 50건
- v0.4 구현 완료 뒤 별도로 수집·동결할 완전 신규 홀드아웃

기존 210건은 회귀와 오류 분석에 사용하되, 최종 승인 판단은 구현 후 새로 동결하는 홀드아웃에서 한다. 최종 홀드아웃의 라벨은 시스템 실행 전에 확정하며 결과를 본 뒤 수정하지 않는다.

승인 게이트는 최소 다음과 같다.

| 게이트 | 승인 기준 |
|---|---:|
| 위험 문구 재현율 | ≥ 85% |
| 안전 문구 오탐률 | ≤ 5% |
| 경고·비판 문맥 high 오탐 | 0건 |
| 핵심 도메인별 위험 탐지율 | 각 ≥ 60% |
| 반복 실행 불일치 | 0건 |
| UI 치명 오류 | 0건 |

추가로 Interpreter JSON 스키마 성공률, review 비율, P95 지연시간, 입력당 평균 비용을 함께 보고한다. 이 운영 지표의 허용치는 프로토타입 측정 후 구현 전에 고정한다.

## 8. 단계별 진행과 중단 조건

1. 오프라인 프로토타입으로 현재 실패 사례의 발화 유형·문맥 구분 가능성을 확인한다.
2. 규칙 결과와 Interpreter JSON을 결합한 평가 전용 파이프라인을 만든다.
3. 기존 210건 회귀를 통과한 뒤 신규 홀드아웃 라벨을 동결한다.
4. 신규 홀드아웃 최종 게이트를 단 한 번 실행하고 승인 여부를 결정한다.
5. 통과한 경우에만 UI와 운영 경로에 제한적으로 연결한다.

이번 최종 실패 이후에는 하이브리드 검증 없이 대규모 규칙 또는 예외 목록을 더 추가하지 않는다. 하이브리드도 동일 게이트를 통과하지 못하면 자동 high 범위를 축소하고 사람 검토 중심 제품으로 재정의한다.
